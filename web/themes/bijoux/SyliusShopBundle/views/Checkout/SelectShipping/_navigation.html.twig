{% set enabled = order.shipments|length %}

{# <div class="ui two column grid">
    <div class="column">
        <a href="{{ path('sylius_shop_checkout_address') }}" class="ui large icon labeled button"><i class="arrow left icon"></i> {{ 'sylius.ui.change_address'|trans }}</a>
    </div>
    <div class="right aligned column">
        <button type="submit" id="next-step" class="ui large primary icon labeled{% if not enabled %} disabled{% endif %} button" {{ sylius_test_html_attribute('next-step') }}>
            <i class="arrow right icon"></i>
            {{ 'sylius.ui.next'|trans }}
        </button>
    </div>
</div> #}

<div class="checkout_page--address--button_cont">
    <a href="{{ path('sylius_shop_checkout_address') }}" class="btn-primary-light checkout_page--address--button_cont--btn"><i class="arrow left icon"></i> {{ 'sylius.ui.change_address'|trans }}</a>
    <button type="submit" id="next-step" class="btn-secondary checkout_page--address--button_cont--btn{% if not enabled %} disabled{% endif %}" {{ sylius_test_html_attribute('next-step') }}>
        {{ 'sylius.ui.next'|trans }}
    </button>
</div>